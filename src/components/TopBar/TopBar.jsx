import React from 'react';
import { Box } from 'rebass/styled-components';

const TopBar = () => <Box>TopBar</Box>;

TopBar.propTypes = {};

TopBar.defaultProps = {
  siteTitle: '',
};

export default TopBar;
