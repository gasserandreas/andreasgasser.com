import preset from '@rebass/preset';

const { colors } = preset;

export default {
  colors: {
    ...colors,
    // text colors
    text: '#333',
    'text-inverse': '#eee',
    muted: '#c9c9c9',
    // background colors
    background: '#656565',
    'background-inverse': '#fff',
  },
};
