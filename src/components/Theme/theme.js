import rebass from '@rebass/preset';

const theme = {
  ...rebass,
  colors: {
    ...rebass.colors,
    white: '#fff',
    black: '#000',
    lightGray: '#ececec',
    purple: '#744c9d',
    text: '#ccc',
  },
};

export default theme;
